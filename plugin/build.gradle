// Versions are pinned to what the Godot 4.7.1 Android build template uses, so
// the plugin and the app agree on Kotlin, AGP and the Java target.
plugins {
	id 'com.android.library' version '8.6.1' apply false
	id 'org.jetbrains.kotlin.android' version '2.1.21' apply false
}
